# AI 智能翻译 · 后端依赖
# 使用: py -3.12 -m venv .venv && .venv\Scripts\activate && pip install -r server\requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.9.0
pillow>=10.4.0
numpy>=1.26.0
mss>=9.0.1
pywin32>=306

# OCR（CPU 可用；安装失败时图片翻译会降级提示）
rapidocr-onnxruntime>=1.3.24

# 视频 ASR（较大，按需；安装失败时视频任务会明确报错）
faster-whisper>=1.0.0

# 备用 ffmpeg 二进制（无系统 ffmpeg 时用；清华源安装快）
imageio-ffmpeg>=0.6.0

# Edge TTS 语音朗读
edge-tts>=7.0.0

# 文件翻译：docx / pdf
python-docx>=1.1.0
pypdf>=4.0.0
