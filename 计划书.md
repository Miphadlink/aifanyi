# AI 智能翻译工具 · 项目计划书

> 版本：v0.2（规划稿，已按「多格式视频 + 游戏拖入启动」修订）  
> 日期：2026/03/09  
> 工作目录：`C:\Users\Miphy\Desktop\aixiangmu`  
> 状态：**已按阶段 0–4 搭建可运行工程**（待配置 API Key / 安装 ffmpeg 后做端到端验收）

---

## 1. 产品定位

面向 Windows 桌面场景的**多模态 AI 翻译工具**，覆盖日常四类高频需求：

| 场景 | 用户痛点 | 本工具方案 |
|------|----------|------------|
| 文字翻译 | 粘贴复制繁琐、术语不准 | 输入即译，可选风格/术语表 |
| 图片翻译 | 外文截图、海报、说明书 | OCR 抽文 → 翻译 → 原位贴回或双语对照 |
| 视频翻译 | 无字幕/字幕不全、格式杂 | 多格式导入 → ASR → 字幕导出（SRT/ASS），可选配音 |
| 游戏翻译 | 实时性要求高、遮挡 UI、启动麻烦 | **拖入游戏启动** → 绑定窗口 → OCR → 浮窗覆盖译文 |

目标用户：内容创作者、学生/科研、游戏玩家、跨境办公人群。  
首期交付形态：**本地桌面应用**（Electron 壳 + Python AI 服务），保证离线可跑核心能力，API 模型可选增强。

---

## 2. 功能清单

### 2.1 共用能力（P0）

- 源语言自动检测 + 目标语言选择（中/英/日/韩起步，可扩展）
- 翻译引擎可切换：云端 LLM API / 本地模型
- 术语表（用户自定义专业词强制译法）
- 历史记录、收藏、一键复制
- 全局快捷键唤起主窗口

### 2.2 文字翻译（P0）

- 单段/多段文本翻译
- 流式输出（打字机效果，降低等待感）
- 保留段落与基础 Markdown 结构
- 划词翻译（系统托盘 + 剪贴板监听，可选）

### 2.3 图片翻译（P0）

- 支持粘贴截图、拖入本地图片、选择区域截图
- OCR 识别文字与位置（bounding box）
- 两种输出：
  1. **对照模式**：左原图 / 右译文列表（默认，稳妥）
  2. **覆盖模式**：把译文绘回原图区域（需简单 inpainting/贴底色）
- 批量处理多张图片

### 2.4 视频翻译（P1，多格式）

**导入与格式**

| 类别 | 扩展名 | 处理说明 |
|------|--------|----------|
| 常见容器 | `.mp4` `.mkv` `.mov` `.avi` `.webm` `.flv` `.wmv` `.m4v` | 一等公民，UI 直接拖入 |
| 流媒体/采集 | `.ts` `.m2ts` `.mts` `.vob` `.mpg` `.mpeg` | 支持；异常容器用 ffmpeg 探测 |
| 带字幕容器 | 含内封字幕的 mkv/mp4 | 优先「只翻译已有字幕轨」；无可用轨再走 ASR |
| 音频误拖入 | `.mp3` `.wav` `.flac` `.aac` `.m4a` `.ogg` | 当作「无画面音轨」处理，只出字幕文件 |
| 批量 | 多文件拖入 | 进入任务队列，按序/可配置并发处理 |

实现要点：

- 入口统一 `ffmpeg -i` 探测：容器、时长、音轨有无、已有字幕轨
- 无音轨视频：仅提示「无语音可识别」，可跳过或只做 OCR 字幕（P2，暂不做画面 OCR 字幕）
- 有音轨：抽 16kHz 单声道 → faster-whisper → 翻译 → 导出
- 导出：`SRT`（默认）、`ASS`（可调字号/描边/底条）、可选「嵌入软字幕」回写 mkv/mp4
- 可选：TTS 译音 + mux 成新视频文件（不覆盖原片）
- 进度条、失败重试、按文件 hash 缓存中间产物（wav / 转写 JSON）

### 2.5 游戏翻译（P1，拖入启动 + 实时覆盖）

**产品主路径（新增核心）**

1. 用户把游戏 **exe / 快捷方式 / 整个游戏文件夹** 拖进软件（或点「打开游戏」选文件）
2. 软件识别类型、校验路径、创建「游戏会话」
3. **由本软件启动游戏进程**（记录 PID、工作目录、启动参数）
4. 等待并绑定游戏主窗口（标题/进程/窗口类）
5. 对该窗口做区域捕获 → OCR → 翻译 → **透明浮窗覆盖原文**
6. 游戏退出或用户结束会话 → 自动停止捕获，清理浮窗

**识别与启动规则**

| 类型 | 典型特征 | 启动策略 |
|------|----------|----------|
| 通用 EXE | 用户拖入 `.exe` | 直接 `CreateProcess`，工作目录设为 exe 所在目录 |
| **RPG Maker** | `Game.exe`、`www/`（MV/MZ）、`Data/`、`Game.rgss3a` 等 | 工作目录=游戏根目录；兼容 RGSS 与浏览器内核版 |
| **Unity** | `UnityPlayer.dll`、`*_Data/`、`GameAssembly.dll`、`UnityCrashHandler` | 启动主 exe；全屏独占时提示改无边框窗口 |
| Godot / 其他引擎 | `*.pck`、`Project.binary` 等 | 尽力识别；识别失败走「手动选窗口」兜底 |
| 快捷方式 | `.lnk` | 解析目标与参数后再启动 |
| 文件夹拖入 | 目录根下唯一/优先 exe | 扫描候选（排除 uninstall、UnityCrashHandler、启动器），让用户确认或自动选主程序 |

**运行时能力**

- 会话面板：游戏名、PID、窗口标题、捕获状态、延迟、FPS/采样率
- 捕获目标：优先 **进程窗口矩形**（随窗口移动/缩放跟随）；失败则全屏/手动框选
- 触发策略：定时（默认 300ms）+ 帧差变化检测，无变化跳过 OCR
- 译文渲染：无边框、置顶、可点击穿透的浮窗；按 OCR box 半透明底条覆盖原文
- 文本策略：相邻行合并、短句缓存（原文 hash → 译文）、对话框区域可手动锁定
- 热键：开始/暂停/隐藏浮窗/锁定区域（默认可改）
- 安全边界：**不注入、不读写内存、不改游戏文件**；仅屏幕层叠加

**性能目标**

- 普通 AVG/RPG 对话：端到端（截屏→显示译文）**P95 ≤ 800ms**
- 空闲（画面无变化）CPU 占用明显下降
- 主 UI 与游戏进程互不拖死；翻译失败可降级为「显示原文 OCR」

**兼容性说明（写入用户文档）**

- 推荐：无边框窗口 / 窗口化；全屏独占可能抓屏失败
- 带描边花体/二次元艺术字 OCR 准确率下降，可调区域或后期换模型
- 需管理员权限才能启动的游戏：软件可请求以管理员重启自身，再代启游戏
- 在线反作弊网游：**明确不支持**，避免账号风险

### 2.6 非目标（首期明确不做）

- 手机端 / 网页 SaaS 多租户
- 游戏进程注入、内存 Hook、解包读资源文本
- 完整视频会议同传
- 训练自有大模型
- 游戏内嵌引擎级翻译（Unity 汉化包、RGSS 脚本替换）

---

## 3. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  桌面壳 Electron（UI / 托盘 / 快捷键 / 浮窗）              │
│  ├─ 文字页  ├─ 图片页  ├─ 视频页  ├─ 游戏页  ├─ 设置      │
│  └─ 游戏浮窗 BrowserWindow（transparent / overlay）      │
└───────────────┬─────────────────────────────────────────┘
                │ HTTP / WebSocket（localhost）
┌───────────────▼─────────────────────────────────────────┐
│  本地服务 FastAPI（Python）                               │
│  ├─ 翻译路由 / 术语表                                     │
│  ├─ OCR 服务（RapidOCR）                                  │
│  ├─ ASR 服务（faster-whisper）                            │
│  ├─ 视频任务队列（多格式 ffmpeg 管线）                     │
│  ├─ 游戏会话管理（启动/绑定窗口/生命周期）                 │
│  └─ 引擎适配层：云端 API / 本地 LLM                       │
└───────────────┬─────────────────────────────────────────┘
                │
        ┌───────┴────────┐
        ▼                ▼
   云端翻译 API      本地推理（可选）
   （OpenAI 兼容）   （Ollama / llama.cpp）
```

模块边界原则：

- **UI 不直接碰模型**：一律走本地 API，方便日后换引擎、加鉴权、做日志。
- **长任务异步化**：视频翻译用任务 ID + 进度推送，避免卡死 UI。
- **游戏浮窗独立进程**：和主窗口解耦，避免翻译计算拖垮界面。
- **游戏启动归 Python 会话层**：Electron 只做拖拽与展示；进程、PID、窗口句柄由后端管理，便于跟随窗口与退出清理。

---

## 4. 技术选型（结合本机环境）

### 4.1 本机环境现状（已检测）

| 项目 | 状态 | 说明 |
|------|------|------|
| Node.js | ✅ v22.19.0（`D:\node`） | 前端/Electron 可用 |
| npm | ✅ 11.16.0 | 可用 |
| Python | ⚠️ PATH 默认 3.7；`py` 有 **3.14 / 3.12** | **必须用 3.12** 跑 AI 栈，3.7 过旧 |
| Git | ✅ 2.54.0 | 可用 |
| Docker | ✅ 已安装 | 可做备选一键环境 |
| GPU | ✅ RTX 4060 Laptop + Intel UHD | 有 NVIDIA 独显 |
| CUDA Toolkit | ✅ 13.3 已装 | 具备本地推理条件 |
| ffmpeg | ❌ **未安装** | **视频/字幕硬依赖，必须补** |
| Tesseract / Whisper CLI / Ollama | ❌ 未装 | 计划用 Python 包替代，Ollama 可选 |
| 磁盘 | C 剩约 88GB / D 剩约 53GB | 够装中等模型；大模型需规划 |

### 4.2 推荐技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 桌面壳 | Electron + Vue 3 + TypeScript + Vite | 你熟悉 Vue/TS；浮窗、托盘、快捷键生态成熟 |
| UI 组件 | Element Plus 或 Naive UI | 中文友好，表单/表格/上传齐全 |
| 本地 API | FastAPI + Uvicorn | 异步、文档自动生成、对接 Python AI 最省事 |
| 文字翻译 | 默认接 **OpenAI 兼容 API**；可选本地 Ollama | 实现快、质量稳；密钥本地配置 |
| OCR | **RapidOCR**（PaddleOCR 推理封装） | 中文效果好、CPU 可跑、安装比完整 Paddle 轻 |
| 图片覆盖译文 | Pillow + 简单底色填充/半透明条 | 首期不做复杂生成式 inpaint |
| ASR | **faster-whisper**（CTranslate2） | 比 openai-whisper 快，支持 4060 GPU |
| 字幕生成 | 自写时间戳对齐 + 导出 SRT/ASS | 逻辑简单可控 |
| 视频处理 | **ffmpeg**（系统安装或静态包） | 多容器探测、抽音轨、字幕封装/软字幕、可选合流 |
| 游戏捕获 | `mss` + Win32 窗口矩形（`pywin32` / `ctypes`） | 绑定进程窗口，跟随移动缩放 |
| 游戏启动 | Python `subprocess` + 类型探测器 | 拖入 exe/lnk/文件夹 → 启动 → 记 PID |
| 游戏浮窗 | Electron `transparent` + `alwaysOnTop` + `setIgnoreMouseEvents` | 标准覆盖方案，无需注入 |
| 引擎识别 | 目录特征（Unity/RPG Maker/Godot）+ 可选手动选择 | 拖入即开，失败可兜底 |
| 任务队列 | 首期 `asyncio` + 内存队列；复杂再上 Redis | 避免过早引入重依赖 |
| 本地 LLM（可选） | Ollama + 小参数模型（如 qwen2.5:7b 量化） | 有 4060，可离线翻译；显存需实测 |
| 打包 | electron-builder（NSIS） | Windows 安装包 |

### 4.3 需要你补装 / 配置的环境（重要）

按优先级：

1. **ffmpeg（必须）**  
   - 视频抽音、字幕封装、可选音视频合成都依赖它。  
   - 安装任选其一：  
     - `winget install Gyan.FFmpeg`  
     - 或下载 [gyan.dev](https://www.gyan.dev/ffmpeg/builds/) 静态包，解压后把 `bin` 加入 PATH  
   - 验证：`ffmpeg -version`

2. **Python 3.12 虚拟环境（必须）**  
   - 不要使用 PATH 里的 3.7。  
   - 建议：  
     ```powershell
     py -3.12 -m venv .venv
     .\.venv\Scripts\Activate.ps1
     python -m pip install -U pip
     ```

3. **翻译 API Key（强烈建议）**  
   - OpenAI 兼容服务即可（OpenAI / DeepSeek / 通义 / Moonshot 等）。  
   - 放在本地 `.env`，**禁止**提交仓库。

4. **Ollama（可选，离线增强）**  
   - 安装后拉取小模型做离线翻译。  
   - 显存 4060 Laptop 约 8GB，建议 7B 4-bit；更大模型可能 OOM。

5. **磁盘规划**  
   - 模型与缓存建议放 **D 盘**（C 盘剩余更紧张）。  
   - 预留：Whisper small/medium 模型 + OCR 模型 + 可选 LLM ≈ 3–15GB。

6. **开发者模式 / 权限**  
   - 游戏浮窗一般无需管理员；若遇某些全屏独占游戏黑屏，首期文档写明「窗口化/无边框窗口化」推荐设置。

---

## 5. 目录与模块规划

```
aixiangmu/
├─ 计划书.md                 # 本文档
├─ package.json              # Electron 前端
├─ electron/                 # 主进程 / 浮窗 / 扡盘
├─ src/                      # Vue 页面与组件
│  ├─ views/TextTranslate.vue
│  ├─ views/ImageTranslate.vue
│  ├─ views/VideoTranslate.vue
│  ├─ views/GameTranslate.vue
│  └─ views/Settings.vue
├─ server/                   # FastAPI 后端
│  ├─ main.py
│  ├─ api/                   # text / image / video / game 路由
│  ├─ services/
│  │  ├─ translator.py       # 引擎适配
│  │  ├─ ocr.py
│  │  ├─ asr.py
│  │  ├─ game_launcher.py    # 拖入识别 + 启动 + PID/窗口
│  │  ├─ game_capture.py     # 窗口矩形截屏 + 帧差
│  │  └─ video_pipeline.py   # 多格式 ffprobe/抽音/字幕
│  ├─ tasks/                 # 视频长任务
│  └─ requirements.txt
├─ .env.example              # API Key、模型路径模板
└─ docs/
   ├─ 环境安装.md
   └─ 游戏翻译使用说明.md     # 引擎兼容表、无边框/管理员说明
```

---

## 6. 实施阶段

### 阶段 0：工程脚手架（0.5–1 天）

- 初始化 monorepo：Electron + Vue3 + FastAPI
- 本地一键启动：`npm run dev` 同时拉起前后端
- 环境检查脚本：检测 Python 版本、ffmpeg、GPU

### 阶段 1：文字翻译（P0，1–2 天）

- 设置页：API Base / Key / 模型名 / 目标语言
- 文字页：输入 → 流式翻译 → 复制 / 历史
- 术语表基础版（简单 JSON 映射，后处理替换）

**验收**：中英互译 200 字内 2s 内出首字（视 API）；历史可查。

### 阶段 2：图片翻译（P0，2–3 天）

- 粘贴/拖拽图片
- RapidOCR 识别 + 翻译
- 对照模式 UI；覆盖模式 MVP（译文画在色块上）

**验收**：中英日截图正确率可用；覆盖模式不严重错位。

### 阶段 3：视频翻译（P1，3–5 天）

- ffmpeg 环境检测与「未安装」引导
- 多格式探测：容器/时长/音轨/已有字幕轨
- 拖入队列：常见视频全支持；音频按「仅字幕」处理
- faster-whisper 转写 + 时间戳 → 翻译 → SRT/ASS
- 可选：软字幕嵌入、TTS+mux（覆盖原片禁止，只写新文件）
- 任务进度 WebSocket 推送；hash 缓存

**验收**：  
- `mp4/mkv/mov/avi/webm/flv/ts` 等至少 7 类容器可进任务  
- 5 分钟中/英视频产出可读 SRT；mkv 可导出 ASS  
- 无音轨文件给出明确提示且不崩溃  

### 阶段 4：游戏翻译（P1，5–8 天，风险最高）

**4a. 拖入启动（先做）**

- 支持拖入：`.exe` / `.lnk` / 游戏文件夹
- 引擎探测：Unity（`UnityPlayer.dll`、`*_Data`）、RPG Maker（`Game.exe`、`Data/`、`www/`）、Godot/未知（兜底）
- 候选 exe 列表（排除 CrashHandler、unins*、启动器）；唯一候选可自动选
- 启动：工作目录=游戏根；记录 PID；等待主窗口；结束时清理

**4b. 实时覆盖**

- 窗口矩形捕获 + 帧差 + 定时 OCR
- 透明浮窗覆盖译文；跟随窗口移动
- 热键、点击穿透、会话面板（延迟/状态）
- 管理员权限代启、全屏独占提示

**验收**：  
- RPG Maker / Unity 示例工程（或用户自备游戏）拖入后可由本软件打开并出译文  
- 窗口化下延迟可感知可用（目标 P95≤800ms）；浮窗可隐藏/关闭会话  
- 游戏退出后捕获自动停止，无残留进程/浮窗  

### 阶段 5：打磨与打包（2–3 天）

- 错误提示、空态、日志
- electron-builder 打 Windows 包
- 使用文档、故障排查（ffmpeg 未装、API 失败、显存不足、全屏独占、管理员权限）
- 游戏格式兼容表（已测引擎/版本）

**里程碑总估**：约 **2.5–3.5 周**（单人、按 P0→P1；游戏拖入启动拉长了阶段 4）。

---

## 7. 关键技术方案说明

### 7.1 翻译引擎适配

```
Translator 接口
  ├─ CloudTranslator   # OpenAI 兼容 chat/completions
  ├─ OllamaTranslator  # 本地 http://127.0.0.1:11434
  └─ （预留）NLLB / M2M100 纯本地小模型
```

统一输入输出：

- 入：`text, source_lang, target_lang, domain/terms, style`
- 出：`translated_text, engine, latency_ms, usage`

失败策略：超时重试 1 次 → 失败展示错误，不静默吞掉。

### 7.2 视频流水线（多格式）

```
拖入文件
  → ffmpeg/ffprobe 探测（容器、时长、流）
  ├─ 无音轨：提示结束（P2 再做画面 OCR）
  ├─ 有已有字幕轨：可选「只翻译字幕」旁路
  └─ 有音轨：抽 16kHz 单声道 wav
        → faster-whisper segments(start,end,text)
        → 逐段翻译（并发 2–4，注意 API 限流）
        → 组装 SRT/ASS
        → 可选：软字幕嵌入 / TTS+ffmpeg 混流（输出新文件）
```

- 坏文件/不支持容器：ffprobe 失败时给出「格式不支持或文件损坏」
- 缓存 key：`file_hash + asr_model + translate_engine + lang_pair`

### 7.3 游戏拖入启动 + 实时覆盖

```
拖入 .exe / .lnk / 文件夹
  → resolve 主程序路径
  → detect_engine()  # Unity | RPGMaker | Godot | Unknown
  → 启动进程（cwd=游戏根）→ PID
  → poll 主窗口（标题/进程）
  → capture_loop（窗口矩形 + 帧差）
       → OCR → merge boxes → translate（短句缓存）
       → WebSocket 推送 box + 译文
  → Electron 浮窗按屏幕坐标覆盖
  → 进程退出 / 用户结束 → 会话清理
```

限制（首期接受并写进文档）：

- 二次元描边/特效字 OCR 可能不稳  
- 全屏独占模式可能抓不到，建议无边框窗口化  
- 极快翻页对话可能闪字，可提高采样或手动锁定区域  
- 多启动器游戏（登录器再拉起主程序）：首期支持「手动重新绑定窗口」  
- 不支持需要注入才能读文本的网游/反作弊游戏  

### 7.4 配置与安全

- 所有密钥只存本地 `.env` / 用户数据目录，UI 可改不可回显明文 Key  
- 服务默认只监听 `127.0.0.1`  
- 不上传用户音视频到第三方（除用户自选的翻译/ASR API）  
- 游戏：不写游戏目录、不删用户文件；仅启动用户主动选择的可执行文件  
- 启动记录（路径、PID）仅本地会话日志，可一键清除

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 未装 ffmpeg | 视频功能不可用 | 阶段 0 环境自检，启动时红条提示安装指引 |
| Python 3.7 被误用 | 依赖装不上 | 固定 `py -3.12` + venv；文档强调 |
| 4060 显存不足 | 本地 LLM OOM | 默认云端翻译；本地模型用小量化；可关 GPU 回退 CPU |
| 游戏反作弊误伤 | 账号风险 | 不注入、不读内存；仅截屏叠加；文档警示；网游不支持 |
| 主程序识别错（启动器/多 exe） | 游戏打不开或开错 | 候选列表让用户确认；会话内可「重新绑定/手动启动」 |
| 全屏独占抓不到窗口 | 无译文 | 文档强制推荐无边框；失败引导切换 |
| 需要管理员权限的 exe | 启动失败 | 检测错误码；支持「以管理员重启本软件后代启」 |
| OCR 对游戏字体差 | 翻译空/错 | 可锁区域；缓存常见 UI 坐标；后续可换模型 |
| 视频格式冷门/损坏 | 任务失败 | ffprobe 预检 + 明确错误文案；尽量用 ffmpeg 兜底转码抽音 |
| API 成本与限流 | 高频游戏翻译烧钱 | 短句缓存、合并识别结果、可切本地模型 |
| Electron 打包体积 | 分发大 | 首期可接受；文档说明；必要时提供「仅前端 + 用户自装 Python」精简包 |

---

## 9. 验收标准（摘要）

- **文字**：粘贴翻译、流式输出、历史与复制正常  
- **图片**：截图 OCR + 对照翻译正确；覆盖模式可读  
- **视频**：至少覆盖 mp4/mkv/mov/avi/webm/flv/ts 等常见多格式；5–10 分钟视频可导出 SRT/ASS，时间轴基本对齐  
- **游戏**：支持拖入 exe / 快捷方式 / 文件夹启动；RPG Maker、Unity 类游戏可绑定窗口实时覆盖译文；热键可关；游戏退出后会话干净结束  
- **工程**：README 可让新机器 15 分钟内跑通 P0 功能  

---

## 10. 建议的首期交付顺序（执行时按此推进）

1. 脚手架 + 环境自检  
2. 文字翻译（打通前后端与 API 配置）  
3. 图片翻译（OCR 链路验证）  
4. 视频字幕（多格式 ffmpeg + Whisper）  
5. 游戏：**先拖入启动与窗口绑定**，再做实时浮窗覆盖  

---

## 11. 已采纳的产品约束（本轮修订）

- 视频必须多格式；以 ffmpeg 为底座，冷门容器尽量兜底  
- 游戏必须支持 **拖入启动**（RPG Maker / Unity 等），由本软件打开后再翻译，而不是只能「先自己开游戏再框选」  
- 仍坚持：不注入进程、不读写游戏内存  

### 仍待你确认

1. **界面语言**：默认简体中文，是否需要英/日界面？  
2. **翻译后端**：是否有可用的 OpenAI 兼容 API？倾向哪家？  
3. **是否首期就要本地离线翻译**（Ollama），还是先云端再补离线？  
4. **游戏会话**：是否需要「最近游戏」启动列表（记住路径一键再开）？  
5. **打包形态**：标准安装包，还是绿色便携目录？

---

## 12. 下一步

你确认第 11 节剩余决策点后，我将按阶段 0 开始搭工程，并在实现过程中更新本文档的功能勾选与偏差说明。
